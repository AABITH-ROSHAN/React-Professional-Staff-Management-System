<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mongoose CRUD</title>
    <link rel="stylesheet" href="css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
    <h1 class="text-center mb-4">Professional Staff Management System</h1>
    
    {{#if message}}
        <div class="alert alert-success">{{message}}</div>
    {{/if}}
    
    <div class="card p-4 shadow-sm">
        {{#if edit_id}}
            <h3 class="text-center">Edit Staff</h3>
            <form action="/update-staff/{{edit_id}}" method="post">
                <div class="mb-3">
                    <label class="form-label">Staff Name:</label>
                    <input type="text" name="Name" value="{{edit_staff.Name}}" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Subject Name:</label>
                    <input type="text" name="Subject" value="{{edit_staff.Subject}}" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Update</button>
            </form>
        {{else}}
            <h3 class="text-center">Add New Staff</h3>
            <form action="/store-staff" method="post">
                <div class="mb-3">
                    <label class="form-label">Staff Name:</label>
                    <input type="text" name="Name" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Subject Name:</label>
                    <input type="text" name="Subject" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-success w-100">Add</button>
            </form>
        {{/if}}
    </div>
    
    <h2 class="mt-5">Staff List...</h2>
    <table class="table table-bordered table-striped mt-3">
        <thead class="table-dark">
            <tr>
                <th>Name</th>
                <th>Subject</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            {{#each staffs}}
                <tr>
                    <td>{{this.Name}}</td>
                    <td>{{this.Subject}}</td>
                    <td class="text-center">
                        <a href="/?edit_id={{this._id}}" class="btn btn-warning btn-sm">Edit</a>
                        <a href="/?delete_id={{this._id}}" onclick="return confirm('Are you sure?')" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>